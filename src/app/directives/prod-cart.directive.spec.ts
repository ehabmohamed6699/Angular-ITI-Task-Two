import { ProdCartDirective } from './prod-cart.directive';

describe('ProdCartDirective', () => {
  it('should create an instance', () => {
    const directive = new ProdCartDirective();
    expect(directive).toBeTruthy();
  });
});
